{
  "name": "@monorepo/tooling",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "build": "run-p preset:build",
    "preset:release": "gulp release -f presets/index.ts",
    "preset:build": "gulp build -f presets/index.ts",
    "preset:publish": "gulp publish -f presets/index.ts"
  },
  "devDependencies": {
    "@types/gulp": "^4.0.18",
    "bumpp": "^11.1.0",
    "gulp": "^5.0.1",
    "gulp-cli": "^3.1.0",
    "npm-run-all": "^4.1.5",
    "sucrase": "^3.35.1",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3"
  }
}
